<h1><%= @page_title = "View All Pickups" -%></h1>
<div id="search">
<% form_tag({}, :method => "GET") do %>
  <%= text_field_tag(:search, (session[@list_name] || {})[:search]) %><%= submit_tag 'Search', :class => 'submit' %>
  <%= link_to "Clear", :search => "" %>
<% end %>
</div>
<% if @pickup_pages.count != 0 -%>

  <%= admin_table({:columns =>[:name, :display, :last_updated],
    :list => @pickups,
    :pages => @pickup_pages
  })%>

<% else -%>

  There are currently no Pickups in the system.

<% end -%>



