<script src="javascripts/prototype.js" type="text/javascript"></script>
<%#= javascript_include_tag "prototype" %>
<script type="text/javascript">

  //window.opener.location.reload();

  function randomString()
  {
    var chars = "0123456789";
    var string_length = 8;
    var randomstring = '';
    for (var i=0; i<string_length; i++)
    {
      var rnum = Math.floor(Math.random() * chars.length);
      randomstring += chars.substring(rnum,rnum+1);
    }
    return randomstring
  }

  function updateImages()
  {
    window.parent.location = window.parent.location;
    //var images = window.parent.document.getElementsByTagName('img');
    //for (index in images)
    //{
    //  try
    //  {
    //    //var temp = images[index].src;
    //    images[index].src = images[index].src.split('?')[0] + '?' + randomString();
    //    //alert("image sourse altered from " + temp + " to " + images[index].src);
    //  }
    //  catch(err)
    //  {
    //    //alert(err);
    //  }
    //}
  }

  updateImages();
  //alert('wooooot');
  self.parent.myLytebox.end();
  //alert('wooooot');

</script>