<div class="top_home_left">
  <div class="top_home_pic">
    <span></span>
    <%= scale_image_tag banner.trip.picture1, :alt => "Don't be a Tourist", :title => "Don't be a Tourist", :border => "0", :hspace => "0", :height => 268, :width => 557, :vspace => "0", :class => "home_main_trip_img" %>
  </div>
</div>

<div class="top_home_right">

  <div class="home_next_trip">
    <!--<h2><%= link_to banner.name, {:controller => :trip, :action => :show, :id => banner.trip.id} %></h2>-->
    <p>
      <%= link_to "#{banner.trip.name}", {:controller => :trip, :action => :show, :id => banner.trip.id}, {:class=>"large_link"} %>
    </p>    
    <p class="date_highlight">
      <%= link_to banner.trip.length_dates, {:controller => :trip, :action => :show, :id => banner.trip.id} %>
    </p>
  </div>
  
  <p align="center">
    <%= link_to "Book Now", {:controller => :trip, :action => :show, :id => banner.trip.id}, {:class=>"book_now", :title=>"Book Online Now"} %>
  </p>
  
</div>    
