/* This function shows/hides the element depending on its current visiblity */function expandCollapse(elem) {	if (document.getElementById(elem).style.display == "none" || document.getElementById(elem).style.display == "none!important") {		showElement(elem);	}	else {		hideElement(elem);	}}/* This is used to hide all the steps except the first */function loadPage(steps) {	var detect = navigator.userAgent.toLowerCase();	var browser = "";		if (detect.indexOf("msie") + 1) {		browser = "ie";	}		document.getElementById("npSteps").className = "noprint"+browser;		if (steps > 0) {		showStep(1, steps);				for (i=1;i<=steps;i++) {			sStep='pStep'+i.toString();			document.getElementById(sStep).className = "print"+browser;		}	}	}/* This shows a specific step by looping through and hiding the nonspecified steps, and showing the specified steps */function showStep(step, steps) {	var i;	var sStep;	var sStepSel;		for (i=1;i<=steps;i++) {		sStep='step'+i.toString();		sStepSel='stepSel'+i.toString();				if (i == step) {			setStepClass(sStep, "visible");			changeStepStyle(sStepSel, true);		}		else {			setStepClass(sStep, "hidden");			changeStepStyle(sStepSel, false);		}	}}/* This sets the class of the specified step (used in the above function) */function setStepClass(elem, visibility) {	var detect = navigator.userAgent.toLowerCase();	var browser = "";		if (detect.indexOf("msie") + 1) {		browser = "ie";	}		if (visibility == "hidden") {		document.getElementById(elem).className = "step"+visibility+"ie";	} else {		document.getElementById(elem).className = "step"+visibility;	}}/* This is used by the expandCollapse code to show an element */function showElement(elem) {	document.getElementById(elem).style.display = "block";}/* This is used by the expandCollapse code to hide an element */function hideElement(elem) {	var detect = navigator.userAgent.toLowerCase();	// msie	if (detect.indexOf("msie") + 1) { document.getElementById(elem).style.display = "none"; }	// other browsers	else { document.getElementById(elem).style.display = "none!important"; }}/* This sets the styles for selected steps and un-selected steps (the clickable step links) */function changeStepStyle(elem, selected) {	var detect = navigator.userAgent.toLowerCase();	var clrBG;	var clrTXT;	if (selected == true) {		clrBG = "#FF6600";		clrTXT = "#FFFFFF";	}	else {		clrBG = "#FFFFFF";		clrTXT = "#FF6600";	}	/* msie */	if (detect.indexOf("msie") + 1) {		document.getElementById(elem).style.backgroundColor = clrBG;		document.getElementById(elem).style.color = clrTXT;	}	/* other browsers */	else {		document.getElementById(elem).style.backgroundColor = clrBG;		document.getElementById(elem).style.color = clrTXT;	}}